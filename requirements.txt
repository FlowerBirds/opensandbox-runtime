# Common Python dependencies for the OpenSandbox runtime image.
# Install with:
#   pip install -r requirements.txt
# Versioning convention:
#   * ==x.y.z  exact pin (security / reproducibility)
#   * >=x.y    minimum version floor (feature guarantee)
#   * (none)   latest compatible release

# --- Data processing & analysis ---
numpy
pandas
openpyxl>=3.1

# --- Office documents ---
python-docx>=1.1      # Word (.docx)
python-pptx>=0.6      # PowerPoint (.pptx)
markitdown>=0.1       # Office/PDF/Image -> Markdown
PyYAML                # YAML config files

# --- Database ---
SQLAlchemy==2.0.51    # SQL toolkit / ORM
PyMySQL==1.2.0        # pure-Python MySQL driver

# --- Crypto (Chinese national standards / 国密) ---
gmssl==3.2.2          # SM2 / SM3 / SM4

# --- HTTP clients ---
requests
httpx

# --- Utilities ---
tqdm
python-dateutil
tabulate
beautifulsoup4
lxml
psutil            # process / system / network introspection
